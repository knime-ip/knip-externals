<project>

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.knime.knip</groupId>
    <artifactId>org.knime.knip.pom-scijava</artifactId>
    <version>1.149</version>
  </parent>

  <artifactId>org.knime.knip.scifio</artifactId>
  <packaging>bundle</packaging>
  <name>Scifio bundle</name>
  <version>${scifio.version}</version>

  <description>This bundle provides an implementation of the SCIFIO library.</description>

  <properties>
    <export.package>io.scif</export.package>
  </properties>

  <dependencies>  
    <dependency>
      <groupId>${scifio.groupId}</groupId>
      <artifactId>scifio</artifactId>
      <version>${scifio.version}</version>
	<exclusions>
	<exclusion>
	<groupId>${imglib2.groupId}</groupId>
	<artifactId>imglib2</artifactId>
	</exclusion>
	<exclusion>
	<groupId>${scijava-common.groupId}</groupId>
	<artifactId>scijava-common</artifactId>
	</exclusion>
	</exclusions>
    </dependency>
  </dependencies>

<build>
    <plugins>
      <plugin>
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <version>2.4.0</version>
        <configuration>
          <instructions>
            <Require-Bundle>org.knime.knip.scijava;bundle-version="${scijava-common.version}", org.knime.knip.imglib2;bundle-version="${imglib2.version}"</Require-Bundle>
          </instructions>
        </configuration>
      </plugin>
    </plugins>
  </build>


	<repositories>
	<repository>
	<id>imagej.public</id>
	<url>http://maven.imagej.net/content/groups/public</url>
	</repository>
	</repositories>
</project>
