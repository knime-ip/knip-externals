<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<properties>
		<bundle.parent>org.knime.knip.tmp</bundle.parent>
		<bundle.name>knip-trackmate</bundle.name>
		<bundle.version>${knip-trackmate.version}</bundle.version>
		<export.contents>*</export.contents>
	</properties>

	<parent>
		<groupId>org.knime.knip</groupId>
		<artifactId>knip-externals</artifactId>
		<version>1.0.0</version>
	</parent>

	<artifactId>${bundle.name}</artifactId>
	<name>${bundle.name}</name>
	<version>${bundle.version}</version>

	<packaging>bundle</packaging>

	<dependencies>
		<dependency>
			<groupId>${bundle.parent}</groupId>
			<artifactId>${bundle.name}</artifactId>
			<version>${bundle.version}</version>
		</dependency>

		<dependency>
			<groupId>org.scijava</groupId>
			<artifactId>scijava-common</artifactId>
			<version>${scijava-common.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imagej</groupId>
			<artifactId>imagej-common</artifactId>
			<version>${imagej-common.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2</artifactId>
			<version>${imglib2.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-realtransform</artifactId>
			<version>${imglib2-realtransform.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-algorithm</artifactId>
			<version>${imglib2-algorithm.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-algorithm-gpl</artifactId>
			<version>${imglib2-algorithm-gpl.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>net.imglib2</groupId>
			<artifactId>imglib2-ij</artifactId>
			<version>${imglib2-ij.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>

		<dependency>
			<groupId>gov.nist.math</groupId>
			<artifactId>jama</artifactId>
			<version>${jama.version}</version>
			<scope>provided</scope>
			<type>bundle</type>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<extensions>true</extensions>
				<configuration>
					<instructions>
						<Require-Bundle>
							imglib2;bundle-version="${imglib2.version}",
							imglib2-algorithm;bundle-version="${imglib2-algorithm.version}",
							imglib2-algorithm-gpl;bundle-version="${imglib2-algorithm-gpl.version}",
							imglib2-realtransform;bundle-version="${imglib2-realtransform-osgi.version}",
							imglib2-ij;bundle-version="${imglib2-ij-osgi.version}",
							imagej-common;bundle-version="${imagej-common.version}",
							scijava-common;bundle-version="${scijava-common.version}",
							jama;bundle-version="${jama.version}"
						</Require-Bundle>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<repositories>
		<repository>
			<id>imagej.public</id>
			<url>http://maven.imagej.net/content/groups/public</url>
		</repository>
	</repositories>

</project>
