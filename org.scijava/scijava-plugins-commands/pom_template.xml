<!-- build an update site -->
<project>
	<modelVersion>4.0.0</modelVersion>

	<properties>
	    <tycho-version>0.22.0</tycho-version>
	</properties>

	<parent>
		<groupId>net.imagej</groupId>
		<artifactId>pom-imagej</artifactId>
		<version>5.3.0</version>
	</parent>

	<groupId>the.group.id.dot</groupId>
	<artifactId>knip-osgi-update-site</artifactId>
	<version>1.0.0</version>
	<!-- we want to build an update site -->
	<packaging>eclipse-repository</packaging>

	<dependencies>
		<!-- SciJava Plugins -->
		<dependency>
			<artifactId>scijava-common</artifactId>
			<version>${scijava-common.version}</version>
		</dependency>
	</dependencies>

	<build>
	   <plugins>
		
		<plugin>
			<groupId>org.eclipse.tycho</groupId>
			<artifactId>tycho-p2-repository-plugin</artifactId>
			<version>${tycho-version}</version>
			<configuration>
			<includeAllDependencies>true</includeAllDependencies>
			</configuration>
		</plugin>
		<plugin>
		      <groupId>org.eclipse.tycho</groupId>
		      <artifactId>tycho-maven-plugin</artifactId>
		      <version>${tycho-version}</version>
		      <extensions>true</extensions>
		</plugin>
		<plugin>
		   <groupId>org.eclipse.tycho</groupId>
		   <artifactId>target-platform-configuration</artifactId>
		   <version>${tycho-version}</version>
		       <configuration>
			  <!-- this line tells tycho to search maven dependencies
				for the osgi bundles/eclipse plugins -->
			  <pomDependencies>consider</pomDependencies>
		       </configuration>
		</plugin>
	   </plugins>
	</build>

</project>

